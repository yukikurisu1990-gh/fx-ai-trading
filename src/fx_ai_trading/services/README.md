# services

ドメインサービス層。各Phaseの中核ロジックをここに段階的に追加する。

想定サブモジュール (将来):
- `data_fetch/` — 価格・ファンダデータ取得 (ブローカー/フィードを抽象化)
- `storage/` — 一次DB / 二次DBへの永続化
- `features/` — 特徴量生成 (テクニカル指標等)
- `prediction/` — モデル推論
- `signal/` — シグナル判定
- `execution/` — 執行補助 / (将来) 自動発注

各サブモジュールは外部依存をInterface化して差し替え可能に保つ。
