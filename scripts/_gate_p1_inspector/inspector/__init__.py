"""Inspector submodules namespace (PR-B.0 stub stage).

PR-B.0 contains ONLY a stub. The real inspection submodules — raw inventory,
coverage, dependency inventory, pipeline feasibility, retention, resolver —
belong to PR-B.1 / PR-B.2 and are intentionally absent here. Their absence is
asserted by tests (no real inspection can be invoked from PR-B.0).
"""
